
'use client'

export interface CoinInfoProp {
    symbol: number,
    name: number,
    price: number,
    changeRate: number,
    marketcap: number,
    volume: number
}
export const CoinInfo = (info: CoinInfoProp) => {
    return (
        <>
            
        </>
    );
};

export default CoinInfo;